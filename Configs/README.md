# Configs

This folder stores simulator UI configuration exported by the app (JSON).

- `layout.json`: window positions/sizes + chart layout
- `hotkeys.json`: hotkey bindings per context

These files are written and read by `Simulator/Simulator.py` via `/api/config/*`.

